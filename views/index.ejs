<section class="card">
  <div class="grid2">
    <div>
      <h2>Multi-Chain Revoke Approvals</h2>
      <p>Hubungkan wallet → pilih chain → scan. Hasil muncul realtime. Klik <b>Revoke</b> di item yang berisiko.</p>
      <button id="connectBtn" class="btn primary">Connect Wallet</button>
      <div id="walletInfo" class="muted mt8"></div>
    </div>
    <div>
      <h3>Pilih Chain</h3>
      <div class="chain-grid" id="chainsBox">
        <% chains.forEach((c)=>{ %>
          <label class="chip">
            <input type="checkbox" value="<%= c.chain_id %>" <%= [1,56,137,42161,10,43114,8453,250].includes(c.chain_id) ? 'checked' : '' %> />
            <span><%= c.chain_name %> (#<%= c.chain_id %>)</span>
          </label>
        <% }) %>
      </div>
      <button id="startScanBtn" class="btn" disabled>Start Scan</button>
    </div>
  </div>
</section>

<section class="card">
  <h3>Hasil Scan</h3>
  <div id="results" class="results"></div>
</section>

<section class="card">
  <h3>Manual Check / Revoke ERC-20</h3>
  <div class="grid2">
    <div>
      <label>Chain ID</label>
      <input id="mChainId" placeholder="1" />
      <label>Token</label>
      <input id="mToken" placeholder="0xToken..." />
      <label>Spender</label>
      <input id="mSpender" placeholder="0xSpender..." />
      <button id="checkAllowanceBtn" class="btn">Check Allowance</button>
    </div>
    <div>
      <div id="manualResult" class="mono"></div>
      <button id="manualRevokeBtn" class="btn danger" style="display:none;">Revoke (approve 0)</button>
    </div>
  </div>
</section>

<script src="/js/main.js"></script>
